server:
  port: 8082

spring:
  application:
    name: course
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/school_course_service
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

  # For map several instances (also set server.port: 0)
  #instance:
  #  instance-id: ${spring.application.name}:${random.uuid}

